# Pruebas y verificación de fase 5

Fecha: 2026-09-17. Rama: `fase-5-validacion-distribucion`.

## Suite y cobertura

La base de partida ejecutó lint, typecheck, test, build y docs:check en verde. La suite totalizó 287 pruebas: API 129, dominio 76, web 39, cliente API 19, validación 14 y movimientos 10. La cobertura de líneas fue API 95.03 %, dominio 94.35 %, movimientos 100 %, validación 93.39 %, cliente API 89.74 % y web 12.43 %.

OpenAPI registró 35 rutas y todos los resultados 2xx con esquema. Los E2E Playwright con proveedor simulado fueron siete y pasaron: `athlete-flow`, `workout-flow`, `ai-flow`, `wod-flow`, `ai-history`, `wod-performance` y `landing-download`. Cuando se encadenan ejecuciones, el límite de diez autenticaciones por minuto puede provocar fallos; para esta evidencia la API se inició con `NODE_ENV=test`, que desactiva ese límite.

## APK, publicación y descarga

La compilación se efectuó con `pnpm --filter @garfit/mobile exec expo prebuild --platform android --clean` y `gradlew assembleRelease`. Tras ajustar Gradle a `-Xmx4096m -XX:MaxMetaspaceSize=1024m`, el resultado fue `BUILD SUCCESSFUL in 17m 24s`. El APK `com.garfit.app` 0.5.0, `versionCode` 5, `minSdkVersion` 24 y `targetSdkVersion` 36 mide 104 931 518 bytes y tiene SHA-256 `e4eabfe20c7dd44d4ef0ca6d448b2ff98f03caf13e4ae7559166cb8f95846bd5`. La firma V2 se verificó con `CN=Android Debug`.

La publicación se realizó con `pnpm --filter @garfit/api release:publish`. La CLI calculó el checksum y tamaño desde el fichero. `GET /releases/android/0.5.0/download` y `GET /releases/android/latest/download` devolvieron 200, 104 931 518 bytes y el mismo SHA-256; se comprobaron `X-Checksum-Sha256`, `Content-Type: application/vnd.android.package-archive` y `Content-Disposition`. Antes de publicar, la descarga estable respondía 404 `NO_RELEASE_PUBLISHED`.

La instalación en dispositivo permanece PENDIENTE: no hubo teléfono ni emulador con imagen de sistema. Se verificaron artefacto, firma, metadatos, versión y checksum descargado.

## Smoke de Gemini

El comando `pnpm test:gemini` concluyó: `SKIPPED — GEMINI_API_KEY not configured`. Por ausencia de clave, la validación con el proveedor real es PENDIENTE y no se afirma ejecución real.
